(setq l1 '(a b c))
(setq l2 '(1 2 3))
(setq tree '(a ( (c d)) (e (fg))))

; you define a function - meaning al aperations in list returns a value
; the result of the last aperation done in the function is returned

; create a list of pairs from l1 and l2
(defun pair (fir sec)) ;defining a function called pair

    (cond ; under different conditions do the following
        ( (not (null fir)) (car fir))  ;if l1 is null return l1

        
    
    
    )